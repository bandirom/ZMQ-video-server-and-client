# ZMQ-video-server-and-client
ZMQ video server and client on Python 3.6 and higher

### Install package:
  pip install --user zmq
  
  pip install --user opencv-python
  
  pip install --user opencv-contrib-python
  
